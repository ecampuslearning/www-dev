# ------------------------------------------------------------------------------
# Devcontainer Environment Configuration (.env.example)
# ------------------------------------------------------------------------------
# This file provides example values for container image metadata and runtime config.
# It is loaded by the Makefile and other scripts.
#
# IMPORTANT: Do NOT use quotes around values. Quoted values may break Docker/Makefile parsing.

## --- Devcontainer Runtime Configuration ---
# Docker host for CLI access inside the devcontainer.
## Setting this variable is optional; the default is usually sufficient unless you need a custom Docker host.
# Default: unix:///var/run/docker.sock (mount the socket into the container for most local setups)
DOCKER_HOST=unix:///var/run/docker.sock

## --- Container User Configuration ---
# Username for the non-root user created inside the container.
# Optional: Default is 'vscode' unless overridden.
DEVCONTAINER_USER=ecampusdev

## --- Author and Maintainer Information ---
# Optional: Defaults to unknown.
IMAGE_AUTHORS=Moses Fabiyi
IMAGE_MAINTAINERS_JSON='[{"name":"ecampusdev","email":"webmaster@ecampuslearning.com"}]'

## --- Vendor Information ---
# Optional: Defaults to unknown.
IMAGE_VENDOR=
IMAGE_VENDOR_URL=https://ecampuslearning.com/

## --- Project Information ---
# Optional: Defaults to unknown.
IMAGE_SOURCE=https://example.com/yo

## --- Optional Metadata ---
# Mark image as deprecated (set to a message or true to indicate deprecation)
#IMAGE_DEPRECATED=
# GitHub Actions Marketplace badge color (e.g. purple, blue, green, yellow, red)
#IMAGE_GITHUB_COLOR=purple
# Comma-separated features/tools included in the image
#IMAGE_FEATURES=git,python,trivy,hadolint,dockle,dive,checkov,syft,grype,goss,ctop,opa,crictl
# Comma-separated keywords for search/discovery (features + general terms)
#IMAGE_KEYWORDS=devcontainer,ubuntu,development,cli,tools,git,python,trivy,hadolint,dockle,dive,checkov,syft,grype,goss,ctop,opa,crictl
# Optional: Human-friendly image title and description
#IMAGE_TITLE=Devcontainer Base Image
#IMAGE_DESCRIPTION=Base image for devcontainers with common CLI tools and developer utilities

## --- Provenance (DO NOT OVERRIDE) ---
# The following values are set directly in the Dockerfile for provenance and MUST NOT be overridden here or in forks:
#   IMAGE_ORIGINAL_VENDOR         # Name of the original project vendor/organization
#   IMAGE_ORIGINAL_VENDOR_URL     # URL of the original vendor (e.g., GitHub profile/org)
#   IMAGE_ORIGINAL_SOURCE         # URL of the original project source repository
#   IMAGE_ORIGINAL_AUTHOR         # Name or handle of the original author
#   IMAGE_LICENSES                # License(s) for the original project
#
# These fields ensure attribution and legal clarity for all downstream images.
#
# You can override any of these values in your local .env file
# except those listed above.